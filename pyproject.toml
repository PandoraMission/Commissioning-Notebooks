[project]
name = "commissioning-notebooks"
version = "0.1.0"
description = ""
authors = [
]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.9,<3.13"
dependencies = [
    "pandorasat (==0.5.23)",
    "pandorapsf (==0.2.11)",
    "pandorasim (==1.0.10)",
    "jupyterlab (>=4.3.5,<5.0.0)",
    "ipywidgets (>=8.1.5,<9.0.0)"
]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
package-mode = false